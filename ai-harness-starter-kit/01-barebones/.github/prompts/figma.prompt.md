---
mode: agent
description: Build a component from a Figma frame via the Figma MCP
---
<!-- Copilot manual command. Uses the Figma MCP from .vscode/mcp.json. -->
Use the Figma MCP to read the selected frame; build a component per frontend rules.
