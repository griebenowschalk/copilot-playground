---
description: Pre-commit gate — lint, typecheck, test, then summarize
---
<!-- Claude legacy command (.claude/commands still works). Invoke with /ship. -->
Run lint + typecheck + test. If green, summarize the change + propose a commit msg. Don't auto-commit.
