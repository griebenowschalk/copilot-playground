# Copy to .env (gitignored). `pnpm setup` does this for you.
DATABASE_URL="file:./dev.db"

# Used by the Figma MCP server and the /figma skill's fetch-context.sh script.
# Get a token at Figma → Settings → Account → Personal Access Tokens.
FIGMA_API_KEY=""
